{
  "name": "semver-tidy",
  "version": "0.1.0",
  "description": "Normalizes messy semantic version strings into canonical semver form.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "semver-tidy": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tsc && node dist/format.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
